[project]
name = "dzweb-orchestrator"
version = "0.1.0"
description = "dzweb: English to Dzongkha web translation middleware (orchestrator)"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "psycopg[binary]>=3.2",
  "redis>=5.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "httpx>=0.27",
  "hypothesis>=6.100",
  "ruff>=0.6",
  "mypy>=1.11",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["orchestrator*"]

[tool.setuptools.package-data]
orchestrator = ["pipeline/golden_corpus.json", "store/migrations/*.sql"]

[tool.pytest.ini_options]
testpaths = ["tests/orchestrator"]
addopts = "-q"
markers = [
  "integration: needs PostgreSQL/Redis (docker compose up -d); run by tools/check.py when reachable",
]
filterwarnings = [
  # third-party: starlette TestClient on httpx/anyio; not our code
  "ignore:Using `httpx` with `starlette.testclient` is deprecated",
  "ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning",
]

[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = ["adapters", "node_modules"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "S"]
ignore = ["S101"]  # assert is how pytest tests work

[tool.mypy]
python_version = "3.11"
strict = true
files = ["orchestrator", "tools"]
