# Engineering Review: dzweb v1 plan

Generated by /plan-eng-review on 2026-09-16
Plan of record: `docs/02-technical-spec.md` + `docs/03-backlog.md`, reconciled issue-by-issue against `docs/designs/dzweb.md` (office hours).
Mode: FULL_REVIEW (scope: full v1, milestone-gated)
Status: review complete; remedies approved; **not yet applied to 02/03** (see Implementation Tasks).

Requirement IDs below are cited as they appear in 02/03. The document defining them is not yet in the repo (see T1).

## Scope decisions

- **Full v1 scope, milestone-gated:** M0 Sprint 0 go/no-go → M1 widget + API + worker (pilot) → M2 reviewer UI + glossary/masker flows.
- **Plan of record:** spec + backlog; office-hours doc is evidence.
- **Audio (E5) moved after the pilot** (4A). **Redis kept** as specified (outside voice rejected).
- **Codex outside review skipped** by choice: internal GovTech docs stay off third-party AI services. Native Claude subagent ran instead.

## Approved remedies

| # | Sev | Finding (source line) | Approved remedy |
|---|---|---|---|
| 1 | P0 | Tier 1 can be served cached MT; lookup order has no tier check (spec :160, key :150-155) | **1A** Tier gate before any lookup; Tier 1 reads approved only; cache values carry origin; `test_fr510_tier1_never_served_cached_mt` |
| 2 | P1 | Widget registry maps one id → one Text node, but segments are blocks; "formatting collapse" needs node removal (spec :94, :391-396, :430) | **2A** id → ordered `Text[]` runs; tag mismatch → block stays English in widget; collapse only for proxy/CMS |
| 3 | P1 | Synchronous live MT in request path against slow upstream; shed requests dropped (spec :160, backlog :98-99) | **3A** ~1.5 s sync budget, then `pending_mt` + enqueue (Postgres `SKIP LOCKED`, visibility-timeout sweeper, unique on active jobs); widget re-requests once after ~8 s |
| 4 | P1 | Audio (sprint 7) before pilot-ready (sprint 8), blocked on DCDD + TTS marks (backlog :318-319, :188) | **4A** Move E5 after pilot; keep FR-330 storage guards now |
| 5 | P1 | Crawler S9.2 is an unhardened server-side fetcher in pilot sprint; `/translate/html` takes `url` (backlog :281, spec :296) | **5A** One shared hardened `ops/fetch.py` for crawler + proxy (enrolled-site resolution, no off-allowlist redirects, IP checks after DNS and on connect, GET/HEAD, stripped cookies, size/time caps); `url` is metadata only; `/cso` gates S9.2 |
| 6 | P1 | Client-supplied tier can lower tier (spec :247, backlog :121) | **6A** effective tier = max(site default, site path/selector rules, request); unknown → Tier 1; `GET /v1/config` selectors; config failure → widget stays English; review-item creation capped |
| 7 | P2 | Global glossary version invalidates everything on publish (spec :153-154 vs backlog :85, :251) | **7A** Per-term glossary fingerprint in key + term→keys index; purge only affected; throttled re-warm; approved rows with changed term → `needs_recheck`, not served; keep spec's `⟦T:n⟧` substitution |
| 8 | P2 | NFR-500 grep vs widget's need for Dzongkha logic (spec :37, :40, :12) | **8A** Two locale homes (`locale/dz.py`, `adapters/widget/locale-dz.ts`); CI grep also catches escape forms; shared JSON fixture keeps break rules equal |
| 9 | P0 | `NUM` regex misses any 4+ digit number without commas; verified: `1500`, `2026`, `17123456` unmasked (spec :117-122) | **9A** Catch-all numeric + word-month dates + BTN/Ngultrum; post-restore leak scan (ASCII + Tibetan digits U+0F20–0F29, emails, URLs); Hypothesis property tests; labelled recall with held-out set |
| 10 | P1 | Restore fails only on missing tokens; duplicates/invented tokens pass (spec :130, backlog :53) | **10A** Exact multiset equality for entities; shared validator with tags; tests per mock mode |
| 11 | P1 | Observer is `childList` only; React/Vue update `nodeValue`; toggle-back writes stale original; async records defeat re-entrancy flag (spec :402, :391) | **11A** Observe `characterData` too; `lastWritten` WeakMap; toggle-back restores only if value still equals `lastWritten`; drop re-entrancy flag |
| 12 | P1 | Key normalises whitespace/NFC but stored output holds real entity bytes (spec :151, :167-170) | **12A** Key on masked normalised text; store masked output only; restore per request from that request's entity map |
| 13 | P2 | `versions.segmentation` bumped by hand (spec :158) | **13A** `pipeline_version` = hash of pattern data + segmentation rules + golden fixture masked output |
| 14 | P2 | Tier column on content-addressed row; approval overwrites MT row; status enum + redundant booleans (spec :170-174, :277-281) | **14A** `segment` / immutable `translation_version` / mutable `review_item`; response = status enum + `origin` |
| 15 | P1 | Tag-integrity ≥99% gate measured against the adversarial mock (backlog :299, :301) | **15A** Mock gate = zero malformed/altered/duplicated output across all modes (10k seeded runs); real endpoint gate = ≥99% tag integrity + 100% entity on frozen eval set, nightly |
| 16 | P1 | Only React CSR fixture; no Vue, no SSR hydration race (backlog :147, spec :404) | **16A** Auto-translate after load + idle + quiet frames; React/Vue × CSR/SSR Playwright fixtures; assert no hydration warnings |
| 17 | P1 | 5 of 10 failure modes untested (spec :427-438, backlog :87) | **17A** `tests/fault/` suite in `make check`: Redis, Postgres, WSO2, worker crash, stampede, SSRF redirect, DNS rebinding; each asserts 200 + source text + metric |
| 18 | P2 | Untestable acceptance criteria; `aria-label` translation harms screen-reader users (backlog :149, :246, :169, :157) | **18A** Pinned WebView matrix; feedback limits (10/h per client hash, 100/day per segment, honeypot); axe-core baseline; translate `alt/title/placeholder` only, exclude ARIA |
| 19 | P2 | Widget registry `Map` retains removed nodes (spec :391, :437) | **19A** WeakMap/WeakRef state pruned on removal; mount/unmount + heap snapshot tests |
| 20 | P2 | Whole-document rescan on every debounced mutation (spec :387, :402) | **20A** Incremental, viewport-first extraction with main-thread yielding; long-task budget test on throttled CPU |
| 21 | P2 | Redis-down path multiplies Postgres and upstream load (spec :433) — medium confidence | **21A** One batched TM query per request; Postgres-side enqueue dedupe + rate cap; in-process LRU |
| O1 | P0 | Placeholder tokens rely on tokenizer changes the project can't make; PUA fallback likely `<unk>` (spec :107, 01-srs :7) | **Sprint 0 go/no-go** before S1.5: operator confirmation, recorded WSO2 calls for ≥3 token formats, fallback-by-cause report, hard gate; mock calibrated from recordings |
| O2 | P0 | Pilot at sprint 8, review workflow 9+ → no approved Tier 1 translations at pilot (backlog :319-320) | **Offline seed before pilot:** export Tier 1 snapshot segments (placeholders locked) → DCDD approval → admin CLI import as `origin=human` with audit rows and multiset validation |
| O3 | P1 | Personal-data controls from office hours absent; S1.7 persists every segment (backlog :86) | **Sprint-4 story, /cso-gated:** `data-dz-private`/`data-dz-skip`, public pages only, path normalisation, N-distinct-clients before persist/MT, retention for unapproved machine rows, hash-only logs |
| O4 | P1 | Browser widget can't hold WSO2 consumer keys; shared quota (spec :227, backlog :111) | **Split:** widget endpoints keyless (WSO2 passthrough if FR-600 requires) with origin + per-origin/IP limits; keys/quotas for server-to-server only; confirm FR-600 wording |
| O5 | P1 | "Same input → same output, cache aggressively" + version-only ETag contradict time-varying states (spec :376, :379) | **Body-hash ETag;** `no-store` on responses with any non-final segment; short private max-age otherwise |
| O6 | P1 | Sprint 1 builds Python DOM extractor the widget pilot doesn't use (backlog :312, spec :60) | **Sprint 1 = TS extractor/encoder + server segment parser/validator;** shared golden JSON fixtures for vitest + pytest; Python `extract.py` + S2.2 move to E8 |
| O7 | P1 | WSO2 capacity unsized; live attempts starve worker; S9.2 duplicated (backlog :319-320) | **Measure in Sprint 0;** token bucket reserving ≥50% for worker; pre-translate pilot pages offline before launch; S9.2 only in E9 |
| O8 | P2 | Requirement IDs defined nowhere, yet DoD names tests after them (01-srs :9, backlog :13, :18) | **Add the real numbered SRS as `docs/00-requirements.md` before Sprint 0;** amend with this review's requirements; CI rejects citations of undefined IDs |
| O9 | P2 | Report-an-error button before `/v1/feedback`; no `segment_key` in response; font blocked on licence (backlog :131, :246, :216; spec :273-281, :364) | **Return `segment_key`;** minimal `/v1/feedback` intake in sprint 4; OFL Tibetan fallback font (e.g. Noto Serif Tibetan, licence confirmed by GovTech) until DDC Uchen licensed |
| O10 | P3 | "TypeScript, no build step" impossible (spec :12, :406) | **Define allowed step:** `tsc` (no downlevel helpers, CI-enforced) → pinned minifier (no bundling) → hash + SRI script; size check on final file |
| — | P2 | Outside voice: defer Redis for pilot | **Rejected:** keep Redis per fixed stack; 21A covers Redis-down |

## NOT in scope

- **Audio / TTS / player (E5):** after the pilot (4A); FR-330 storage guards stay.
- **Proxy (E8) and CMS plugins:** after the pilot, behind the shared hardened fetcher and `/cso`.
- **Warm-cache crawler (S9.2):** after the pilot; pilot pre-warms offline from snapshots (O7).
- **Reviewer UI (E7):** after the pilot; pilot uses the offline seed (O2).
- **Corpus export:** deferred until a publication decision (office hours).
- **Fine-tuning or tokenizer changes to the NMT model:** out of project remit (`01-srs.md:7`); Sprint 0 decides a token format that works without them.
- **Codex outside review:** declined to keep GovTech docs off third-party AI services.

## What already exists

Greenfield: no code in the repo. Reused rather than rebuilt:
- Postgres `FOR UPDATE SKIP LOCKED` as the job queue instead of a new queue service (3A).
- Python standard library (`hashlib`, `re`, `unicodedata`) and Hypothesis for masking tests.
- Jinja2 autoescaping for the future reviewer UI.
- translate-toolkit "placeables" as a pattern reference only (not a dependency).
- Office-hours design `docs/designs/dzweb.md`: Phase 0 gate, run-to-node mapping, stale-response guard, personal-data controls, tier authority — folded in via 2A, 6A, 11A, O1, O3.

## Failure modes

| New codepath | Realistic production failure | Test | Handling | User sees |
|---|---|---|---|---|
| Tier gate (1A/6A) | Config endpoint down; widget can't learn Tier 1 selectors | 6A test | Widget offers no translation | English page |
| Job queue (3A) | Worker crashes mid-job; job stays claimed | 17A | Visibility-timeout sweeper | English until retried |
| Glossary purge (7A) | Term publish purges thousands of keys during peak | 17A stampede | Throttled re-warm + shed to queue | English on affected blocks |
| Masker (9A) | New ID format not covered by patterns | 9A property test + leak scan | Leak scan → `entity_check_failed` | English block (never wrong number) |
| Restore (10A/12A) | Model duplicates an entity token | 10A mock modes | Multiset mismatch → source text | English block |
| Widget observer (11A) | Host re-renders fee while Dzongkha on | 11A React fixture | Re-extract; toggle respects host value | Correct current value |
| Widget hydration (16A) | Saved preference fires before SSR hydration | 16A SSR fixtures | Wait for load + idle + quiet frames | No flicker/errors |
| Fetcher (5A) | DNS rebinding to internal IP | 17A | Re-check IP on connect | Crawl skipped, logged |
| Quota manager (O7) | Traffic spike exhausts live tokens | O7 test (to write) | Live skips straight to enqueue | English, warms in background |
| Offline seed import (O2) | Spreadsheet edit breaks a placeholder | Import multiset validation | Row rejected with reason | Tier 1 stays English + notice |
| HTTP caching (O5) | CDN caches a pending response | O5 test | `no-store` on non-final | Translation appears on re-request |

**Critical gaps (no test, no handling, silent):** none remaining after the approved remedies. The quota manager (O7) test still needs to be written as part of its task.

## Worktree parallelization

| Step | Modules touched | Depends on |
|---|---|---|
| S0 Sprint 0 go/no-go | tests/fixtures, tools/ | T1 (SRS), WSO2 access |
| W1 TS extractor/encoder + locale-dz | adapters/widget/ | S0 token format |
| P1 Server segment parser + validator + masker | orchestrator/pipeline/ | S0 token format |
| P2 Cache/TM/data model/keys | orchestrator/store/ | P1 |
| G1 Tier authority + config + personal data | orchestrator/governance/, api/ | P2 |
| Q1 Job queue + quota manager + WSO2 client | orchestrator/pipeline/translate.py, ops/ | P1 |
| F1 Fault-injection suite | tests/fault/ | P2, Q1 |
| W2 Widget observer/hydration/perf | adapters/widget/ | W1 |
| R1 Offline seed export/import CLI | ops/, store/ | P2 |

- Lane A: W1 → W2 (adapters/widget/)
- Lane B: P1 → P2 → G1 (pipeline/, store/, governance/)
- Lane C: Q1 (translate.py, ops/) — **conflict flag:** touches `pipeline/` with Lane B; coordinate or run after P1 merges.
- Lane D: R1 after P2; F1 after P2 + Q1.
- Order: S0 first. Then A + B in parallel worktrees. C after P1 merges. D last.

## Implementation Tasks

- [ ] **T1 (P1, human: ~1 day / CC: ~20min)** — docs — Add numbered SRS as `docs/00-requirements.md`; amend with this review's requirements; CI rejects undefined IDs
  - Surfaced by: Outside voice O8
  - Files: docs/00-requirements.md, docs/01-srs.md, CI config
  - Verify: CI check fails on a test citing an undefined ID
- [ ] **T2 (P1, human: ~1 week / CC: ~1 day)** — pipeline — Sprint 0 go/no-go: token formats, recorded WSO2 calls, capacity, fallback-by-cause report
  - Surfaced by: O1, O7
  - Files: docs/03-backlog.md, tests/fixtures/mt-replay/, tools/phase0/
  - Verify: report produced; gate decision recorded
- [ ] **T3 (P1, human: ~2h / CC: ~15min)** — docs — Apply all approved remedies to `02-technical-spec.md` and `03-backlog.md` (sprint order, stories, acceptance criteria)
  - Surfaced by: all sections
  - Files: docs/02-technical-spec.md, docs/03-backlog.md
  - Verify: each row in "Approved remedies" traceable to a spec section or story
- [ ] **T4 (P1, human: ~4h / CC: ~20min)** — governance — Tier gate before lookup + server-authoritative tier + config endpoint (1A, 6A)
  - Files: orchestrator/governance/tiers.py, orchestrator/api/
  - Verify: `pytest -k fr510`
- [ ] **T5 (P1, human: ~2 days / CC: ~1h)** — pipeline — Masker catch-all + leak scan + multiset validator + masked storage + derived pipeline version (9A, 10A, 12A, 13A)
  - Files: orchestrator/pipeline/protect.py, restore.py, store/cache.py, store/tm.py
  - Verify: `make check` entity gate; Hypothesis suite
- [ ] **T6 (P1, human: ~2 days / CC: ~1h)** — widget — Run-to-node mapping, characterData observer, WeakMap state, incremental extraction, hydration wait (2A, 11A, 16A, 19A, 20A)
  - Files: adapters/widget/
  - Verify: `vitest`; Playwright React/Vue CSR+SSR fixtures
- [ ] **T7 (P1, human: ~2 days / CC: ~1h)** — ops — Job queue, quota manager, shared hardened fetcher (3A, O7, 5A)
  - Files: orchestrator/pipeline/translate.py, orchestrator/ops/fetch.py
  - Verify: `tests/fault/`
- [ ] **T8 (P1, human: ~3 days / CC: ~1.5h)** — tests — Fault-injection suite + split release gates (17A, 15A)
  - Files: tests/fault/, CI config
  - Verify: `make check`
- [ ] **T9 (P1, human: ~2 days / CC: ~1h)** — governance — Personal-data controls story (O3)
  - Files: adapters/widget/, orchestrator/api/, orchestrator/store/
  - Verify: private page → no request; one-off string never reaches WSO2 mock; `/cso`
- [ ] **T10 (P1, human: ~3 days + reviewer time / CC: ~1h)** — ops — Offline Tier 1 seed export/import (O2)
  - Files: orchestrator/ops/seed.py
  - Verify: import rejects broken placeholders; audit rows written
- [ ] **T11 (P2, human: ~1 day / CC: ~30min)** — store — Normalised data model + per-term glossary fingerprint (14A, 7A)
  - Files: orchestrator/store/, migrations/
  - Verify: one-term publish invalidates exactly its segments
- [ ] **T12 (P2, human: ~1 day / CC: ~30min)** — api — WSO2 public/server split, ETag/no-store, `segment_key`, minimal feedback, batched TM read (O4, O5, O9, 21A)
  - Files: orchestrator/api/
  - Verify: pending response never 304'd; feedback limits enforced
- [ ] **T13 (P2, human: ~4h / CC: ~20min)** — widget/build — Locale homes + escape-aware grep, defined build step, OFL fallback font, concrete acceptance criteria (8A, O10, O9, 18A)
  - Files: adapters/widget/, CI config, docs/03-backlog.md
  - Verify: CI size + grep checks

## Completion summary

- Step 0: Scope Challenge — scope accepted as-is (full, milestone-gated)
- Architecture Review: 8 issues found
- Code Quality Review: 6 issues found
- Test Review: diagram produced, 11 gaps identified (4 issues raised)
- Performance Review: 3 issues found
- NOT in scope: written
- What already exists: written
- TODOS.md updates: 3 proposed; repo created and pushed (1), root CLAUDE.md pointer built now (2), Codex review added to TODOS.md (3)
- Failure modes: 0 critical gaps flagged
- Outside voice: ran (Claude subagent, 10 findings; 9 accepted, 1 rejected); Codex skipped by choice
- Parallelization: 4 lanes, 2 parallel / 2 sequential
- Lake Score: 30/30 recommendations chose complete option
- Unresolved decisions: 0

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Outside Review | Claude subagent (native fallback; Codex skipped by user) | Independent 2nd opinion | 1 | skipped (outside) / completed (native) | 10 findings, 9 accepted, 1 rejected |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | issues_open | 21 issues + 10 outside, 0 critical gaps |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **OUTSIDE COVERAGE:** codex, plan-review, skipped (user declined sending GovTech docs to OpenAI); native Claude subagent completed.
- **VERDICT:** Eng review complete with all findings resolved by approved remedies; remedies not yet applied to 02/03 (T3). Not cleared for implementation until T1–T3 land. eng review required after T3.

NO UNRESOLVED DECISIONS
